{
  "name": "cna-modeling-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "mkdirp public && cp -r icon public/icon && vite build",
    "ghpages": "mkdirp public && cp -r icon public/icon && vite build --base=/CNA-ModelingApp/",
    "preview": "vite preview"
  },
  "devDependencies": {
    "mkdirp": "^1.0.4",
    "vite": "^3.1.0"
  }
}
